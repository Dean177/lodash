define(['./function/after', './function/ary', './function/before', './function/bind', './function/bindAll', './function/bindKey', './function/curry', './function/curryRight', './function/debounce', './function/defer', './function/delay', './function/flip', './function/memoize', './function/modArgs', './function/modArgsSet', './function/negate', './function/once', './function/partial', './function/partialRight', './function/rearg', './function/rest', './function/spread', './function/throttle', './function/unary', './function/wrap'], function(after, ary, before, bind, bindAll, bindKey, curry, curryRight, debounce, defer, delay, flip, memoize, modArgs, modArgsSet, negate, once, partial, partialRight, rearg, rest, spread, throttle, unary, wrap) {
  return {
    'after': after,
    'ary': ary,
    'before': before,
    'bind': bind,
    'bindAll': bindAll,
    'bindKey': bindKey,
    'curry': curry,
    'curryRight': curryRight,
    'debounce': debounce,
    'defer': defer,
    'delay': delay,
    'flip': flip,
    'memoize': memoize,
    'modArgs': modArgs,
    'modArgsSet': modArgsSet,
    'negate': negate,
    'once': once,
    'partial': partial,
    'partialRight': partialRight,
    'rearg': rearg,
    'rest': rest,
    'spread': spread,
    'throttle': throttle,
    'unary': unary,
    'wrap': wrap
  };
});
